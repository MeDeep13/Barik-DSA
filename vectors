making a 2d vector of size mxn with all elements -1 : vector<vector<int>> vec(m,vector<int>(n,-1));

making a vector of pair: vector<pair<int,int>> p;
pushing in it: p[i]= {nums[i],i};

instead of using pair<int,int> everywhere we can use the alias using int1= pair<int,int>;

#define vi vector<int>
#define vvi vector<vi>

vector<vector<vector<int>>> dp(n+1,vector<vector<int>>(m+1,vector<int>(l+1,-1)) ==> vector<vvi>dp(n+1,vvi(m+1,vi(3,-1)));


